const enhancer = require('./enhancer.js');

const { repair, succeed, fail } = require('./enhancer.js');

const item = {
  name: 'Burt',
  enhancement: 13, // number from 0 to 20
  durability: 76   // number from 0 to 100
}
// test away!

describe('repair', () => {
  it('', () => {

  })
});

describe('succeed', () => {
  it('', () => {

  })
});

describe('fail', () => {
  it('', () => {

  })
});